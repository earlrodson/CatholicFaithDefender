# Test environment variables | 
NODE_ENV=test
PORT=5002  # Different port for tests to avoid conflicts

# Database configuration for tests
DATABASE_URL=sample
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=table

# Authentication
NEXTAUTH_SECRET=test-secret-key
NEXTAUTH_URL=http://localhost:5002

